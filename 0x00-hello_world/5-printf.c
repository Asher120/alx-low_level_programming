#include <stdio.h>
/**
 * main - Stdout is an integer
 * Description: Takes stdinput and prints a string on display
 * Return: Always 0
 */
int main(void)
{
	printf("With proper grammar,  but the output is a piece of art,\n");
	return (0);
}
